<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alnite.sportskuy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportSkuy">
        <activity android:name=".fragments.BookActivity"></activity>
        <activity android:name=".SearchresActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".Whatsapp" />
        <activity android:name=".Tes1" />
        <activity android:name=".BookingActivity" />
        <activity android:name=".FavoritActivity" />
        <activity android:name=".SetelanActivity" />
        <activity android:name=".FutsalActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".BerandaActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>